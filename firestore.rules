rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // DANGER: Use ONLY for development
      // This rule allows anyone with your project ID to read/write all data
      allow read, write: if true;
    }
  }
}
